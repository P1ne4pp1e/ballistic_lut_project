# 弹道查表项目 - 文件结构和初始化指南

## 完整项目目录结构

```
ballistic_lut_project/
│
├── 📄 README.md                    # 项目主文档
├── 📄 requirements.txt             # Python依赖列表
├── 📄 config.yaml                  # 全局配置文件
├── 📄 .gitignore                   # Git忽略规则
│
├── 📁 src/                         # 源代码主目录
│   ├── 📄 __init__.py
│   │
│   ├── 📁 physics/                 # 物理模型模块
│   │   ├── 📄 __init__.py
│   │   ├── 📄 constants.py         # 物理常数定义
│   │   └── 📄 config.py            # 物理参数配置管理
│   │
│   ├── 📁 integrator/              # 数值积分模块
│   │   ├── 📄 __init__.py
│   │   ├── 📄 rk4.py               # GPU加速RK4积分器
│   │   └── 📄 trajectory.py        # 单条轨迹计算
│   │
│   ├── 📁 lut/                     # 查表生成和查询模块
│   │   ├── 📄 __init__.py
│   │   ├── 📄 generator.py         # 查表生成器
│   │   ├── 📄 query.py             # 查表查询引擎
│   │   └── 📄 storage.py           # HDF5存储管理
│   │
│   └── 📁 utils/                   # 工具模块
│       ├── 📄 __init__.py
│       ├── 📄 logger.py            # 日志系统
│       └── 📄 timer.py             # 计时工具
│
├── 📁 scripts/                     # 可执行脚本目录
│   ├── 📄 generate_lut.py          # 生成查表脚本
│   ├── 📄 validate_lut.py          # 验证查表脚本
│   └── 📄 benchmark.py             # 性能基准测试脚本
│
├── 📁 tests/                       # 单元测试目录
│   ├── 📄 __init__.py
│   ├── 📄 test_physics.py          # 物理参数单元测试
│   ├── 📄 test_integrator.py       # 积分器单元测试
│   ├── 📄 test_lut.py              # 查表功能单元测试
│   └── 📄 test_performance.py      # 性能单元测试
│
├── 📁 data/                        # 数据目录（不追踪git）
│   ├── trajectory_lut.h5           # 生成的查表文件 (~125MB)
│   ├── backups/                    # 查表备份目录
│   └── configs/
│       └── default_config.yaml     # 默认配置备份
│
├── 📁 docs/                        # 文档目录
│   ├── 📄 README.md                # 文档首页
│   ├── 📄 USAGE.md                 # 详细使用指南
│   ├── 📄 API.md                   # API文档
│   ├── 📄 DEVELOPMENT.md           # 开发指南
│   ├── 📄 MATH.md                  # 数学推导
│   └── 📁 images/                  # 文档图片
│
└── 📁 logs/                        # 运行日志目录（不追踪git）
    ├── generate_lut.log            # 生成日志
    ├── validate_lut.log            # 验证日志
    └── benchmark.log               # 基准测试日志
```

## 核心模块功能说明

### 🔧 src/physics/
**职责**: 物理参数管理

| 文件 | 功能 |
|-----|------|
| `constants.py` | 定义所有物理常数（g, ρ, Cd等） |
| `config.py` | `PhysicsConfig` 和 `LUTConfig` 类，配置管理 |

**关键类**:
- `PhysicsConfig`: 封装球体、环境参数
- `LUTConfig`: 查表范围和分辨率配置

### ⚡ src/integrator/
**职责**: 数值积分和轨迹计算

| 文件 | 功能 |
|-----|------|
| `rk4.py` | GPU加速的RK4积分器 `GPUTrajectoryIntegrator` |
| `trajectory.py` | 单条轨迹计算（可选，目前集成在rk4.py中） |

**关键方法**:
- `integrate_trajectory(v0, theta)`: 积分单条轨迹

### 📊 src/lut/
**职责**: 查表生成和查询

| 文件 | 功能 |
|-----|------|
| `generator.py` | 批量生成281,101条轨迹 `TrajectoryLUTGenerator` |
| `query.py` | 快速查表查询 `TrajectoryLUT` |
| `storage.py` | HDF5文件管理（可选扩展） |

**关键类**:
- `TrajectoryLUTGenerator`: 生成和存储查表
- `TrajectoryLUT`: 加载和查询查表

### 🛠️ src/utils/
**职责**: 通用工具函数

| 文件 | 功能 |
|-----|------|
| `logger.py` | 配置日志系统 |
| `timer.py` | 性能计时工具 |

---

## 数据流向图

```
┌─────────────────────────────────────────────────────────────┐
│                     物理参数                                 │
│        (physics/config.py, physics/constants.py)            │
└────────────┬────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────┐
│              RK4数值积分                                    │
│        (integrator/rk4.py)                                  │
│  - 对每个(v0, θ)对积分一条轨迹                            │
│  - 输出：轨迹点 [(d₀,h₀), (d₁,h₁), ...]                  │
└────────────┬────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────┐
│            查表生成器 (离线)                                │
│    (lut/generator.py) - TrajectoryLUTGenerator             │
│  - 循环生成281,101条轨迹                                  │
│  - 写入HDF5文件 (data/trajectory_lut.h5, ~125MB)         │
└────────────┬────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────┐
│            查表文件存储                                    │
│        (data/trajectory_lut.h5)                            │
│  - 元数据: v0_list, theta_list, 参数范围                  │
│  - 轨迹数据: 281,101个数据集                              │
└────────────┬────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────┐
│            查表查询引擎 (在线)                              │
│        (lut/query.py) - TrajectoryLUT                      │
│  - 加载查表到内存                                          │
│  - 实时查询: query(d_target, h_target, v0)               │
│  - 返回: (theta_deg, error_m)                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 快速开始 (5分钟)

### Step 1: 创建项目目录结构

```bash
# 创建项目目录
mkdir -p ballistic_lut_project
cd ballistic_lut_project

# 创建子目录
mkdir -p src/{physics,integrator,lut,utils}
mkdir -p scripts tests data logs docs

# 创建 __init__.py
touch src/__init__.py
touch src/physics/__init__.py
touch src/integrator/__init__.py
touch src/lut/__init__.py
touch src/utils/__init__.py
touch tests/__init__.py
```

### Step 2: 复制代码文件

将生成的代码文件复制到对应目录：

```
src/physics/
  ├── constants.py       (来自第一个artifact)
  └── config.py          (来自第一个artifact)

src/integrator/
  ├── rk4.py             (来自第一个artifact)
  └── __init__.py

src/lut/
  ├── generator.py       (来自第一个artifact)
  ├── query.py           (来自第一个artifact)
  ├── storage.py         (可选)
  └── __init__.py

src/utils/
  ├── logger.py          (来自第一个artifact)
  ├── timer.py           (可选)
  └── __init__.py

scripts/
  ├── generate_lut.py    (来自第一个artifact)
  └── validate_lut.py    (来自第一个artifact)
```

### Step 3: 复制配置文件

```bash
# 复制配置文件
cp config.yaml .
cp requirements.txt .
cp .gitignore .
cp README.md .
```

### Step 4: 安装依赖

```bash
# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/Mac
# 或
venv\Scripts\activate     # Windows

# 安装依赖
pip install -r requirements.txt
```

### Step 5: 生成查表

```bash
python scripts/generate_lut.py
# 等待 30-60 秒（取决于GPU性能）
```

### Step 6: 验证查表

```bash
python scripts/validate_lut.py
# 应该看到：✓ 满足250Hz要求: ✓ 是
```

---

## 文件清单 (复制这些代码)

### 来自第一个artifact (ballistic_lut_project)

```
✅ src/physics/constants.py
✅ src/physics/config.py
✅ src/integrator/rk4.py
✅ src/lut/generator.py
✅ src/lut/query.py
✅ scripts/generate_lut.py
✅ scripts/validate_lut.py
```

### 来自第二个artifact (ballistic_project_config)

```
✅ config.yaml
✅ requirements.txt
✅ .gitignore
✅ README.md
✅ docs/USAGE.md
```

### 手动创建 (简单)

```
✅ src/__init__.py           (空文件)
✅ src/physics/__init__.py   (空文件)
✅ src/integrator/__init__.py (空文件)
✅ src/lut/__init__.py       (空文件)
✅ src/utils/__init__.py     (空文件)
✅ tests/__init__.py         (空文件)
```

---

## 关键文件说明

### 📄 config.yaml
**用途**: 全局配置（物理参数、查表范围、系统设置）

修改项:
- `physics.radius`: 弹丸半径
- `lut.v0_min/max`: 速度范围
- `lut.theta_min/max`: 角度范围
- `system.device`: cuda or cpu

### 📄 requirements.txt
**用途**: Python依赖管理

安装: `pip install -r requirements.txt`

### 📄 .gitignore
**用途**: Git版本控制排除规则

重点排除:
- `data/*.h5` (查表文件太大)
- `logs/` (运行日志)
- `__pycache__/` (Python缓存)

---

## 常用命令

```bash
# 生成查表
python scripts/generate_lut.py

# 验证查表
python scripts/validate_lut.py

# 性能测试
python scripts/benchmark.py

# 运行单元测试
pytest tests/ -v

# 生成覆盖率报告
pytest tests/ --cov=src --cov-report=html

# 代码检查
flake8 src/

# 代码格式化
black src/
```

---

## 项目配置流程

### 物理参数调整

**文件**: `config.yaml`

```yaml
physics:
  radius: 0.0085      # 修改弹丸半径 (m)
  mass: 0.005         # 修改弹丸质量 (kg)
  cd: 0.47            # 修改阻力系数
```

**然后重新生成**: `python scripts/generate_lut.py`

### 查表范围调整

**文件**: `config.yaml`

```yaml
lut:
  v0_min: 10.0        # 最小速度
  v0_max: 30.0        # 最大速度
  dv0: 0.05           # 速度间隔 ← 减小以提高精度（需更长生成时间）
  
  theta_min: -10.0    # 最小仰角
  theta_max: 60.0     # 最大仰角
  dtheta: 0.1         # 角度间隔 ← 减小以提高精度（需更长生成时间）
```

---

## 项目规模统计

| 项 | 数值 |
|----|------|
| 总文件数 | ~25 |
| 源代码行数 | ~800 |
| 轨迹条数 | 281,101 |
| 查表文件大小 | ~125 MB |
| 生成耗时 | 30-60 秒 (GPU) |
| 查询耗时 | 0.5 ms |
| 查询频率 | 2000+ Hz |
| 内存占用 | ~100 MB |

---

## 下一步

1. ✅ 创建目录结构
2. ✅ 复制代码文件
3. ✅ 安装依赖
4. ✅ 生成查表
5. ✅ 验证查表
6. 📝 集成到ROS系统
7. 🔧 性能调优
8. 📚 编写文档

需要我补充任何特定的集成代码吗（如ROS、实时系统等）？